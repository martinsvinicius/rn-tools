import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000',
    alignItems: 'center',
    justifyContent: 'center',
  },

  token: {
    color: '#8257e6',
    fontSize: 20,
    textAlign: 'center',
    marginVertical: 40,
  },

  defaultText: {
    fontSize: 20,
    color: '#fff',
    fontWeight: 'bold',
    textAlign: 'center'
  }
});
